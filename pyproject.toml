[project]
name = "crossy-ia"
version = "1.0.0"
description = "Simulation Crossy Road découplée de l'affichage + comparatif d'IA (heuristique, recherche prédictive, neuroévolution)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2",
    "pygame>=2.6.1",
]

[tool.uv]
package = false
